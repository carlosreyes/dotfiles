- clean: ['~']

- link:
    ~/.files: ''
    ~/.tmux.conf: .tmux.conf
    ~/.bashrc: .bashrc
    ~/.vim: vim/
    ~/.vimrc: .vimrc
    ~/.vimperatorrc: .vimperatorrc

- shell:
  - [git submodule update --init --recursive --remote]
